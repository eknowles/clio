{
  "name": "clio",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run --parallel build",
    "cli": "node ./packages/cli/bin/clio"
  },
  "dependencies": {
    "lerna": "^3.16.4"
  }
}
